import {ReactFlowComponent} from "../ReactFlow/ReactFlowComponent";

export const RightContentComponent = (props) => {
    return (
        <ReactFlowComponent
            {...props}
       />
    )
}
